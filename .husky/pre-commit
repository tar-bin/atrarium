# Run lint-staged for staged files
pnpm exec lint-staged

# Run TypeScript type checking for implementation code only
# (Constitution v1.4.0 Principle 7: Test code MAY have type errors during TDD)
echo "Running TypeScript type checking (implementation code only)..."
pnpm --filter shared/contracts typecheck
pnpm --filter client exec tsc --project tsconfig.app.json --noEmit --skipLibCheck || true
pnpm --filter server typecheck || echo "⚠ Type errors in server (test files allowed per Constitution v1.4.0)"
