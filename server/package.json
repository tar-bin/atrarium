{
  "name": "@atrarium/server",
  "version": "0.1.0",
  "description": "Atrarium server (Cloudflare Workers backend)",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:pds": "vitest run --config vitest.pds.config.ts",
    "typecheck": "tsc --project tsconfig.build.json --noEmit --skipLibCheck",
    "codegen": "npx @atproto/lex-cli gen-api src/schemas/generated/ ../lexicons/*.json"
  },
  "dependencies": {
    "@atproto/api": "^0.13.35",
    "@atproto/identity": "^0.4.3",
    "@atproto/lexicon": "^0.5.1",
    "@atproto/xrpc": "^0.7.5",
    "@atproto/xrpc-server": "^0.7.4",
    "@atrarium/contracts": "workspace:*",
    "@orpc/openapi": "^1.9.3",
    "@orpc/server": "^1.9.3",
    "@orpc/zod": "^1.9.3",
    "hono": "^4.6.14",
    "jose": "^5.9.6",
    "multiformats": "^13.4.1",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@atproto/lex-cli": "^0.9.5",
    "@cloudflare/vitest-pool-workers": "^0.5.30",
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.2",
    "glob": "^11.0.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.94.0"
  }
}
